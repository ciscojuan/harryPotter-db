import { Spells } from "../interfaces/spells";

interface Props{
    spells: Spells[]
}
export default function SpellCard({spells}: Props) {
    //const {id, name, description} = spells
  return (
    <div>
      <h1>Hello Page</h1>
        
    </div>
  );
}